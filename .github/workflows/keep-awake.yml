name: Keep Render Awake
on:
  schedule:
    - cron: '*/12 * * * *' # Runs every 12 minutes
  workflow_dispatch: # Allows you to trigger it manually

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -s https://website-55cx.onrender.com/ping
